package game

pkgs = sdl2, contrib, containers, box2d

--makefile = Makefile

sourcedir = src

modules = Draw, Col, Scene, Events, Objects, Input, Physics, Vector2D, Util

main = Main

executable = game
